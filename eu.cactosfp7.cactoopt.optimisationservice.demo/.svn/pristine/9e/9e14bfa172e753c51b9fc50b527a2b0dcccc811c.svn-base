package eu.cactosfp7.cactoopt.optimisationservice.demo;

import java.util.Dictionary;

import eu.cactosfp7.cactoopt.optimisationservice.config.IOptimisationConfigurable;

/**
 * Configurable for Causa algorithm.
 * @author stier, jakub
 *
 */
public class DemoOptimisationConfigurable implements IOptimisationConfigurable {
	
//	public static final String ALGORITHM_KEY = "algorithm";
//	public static Algorithm chosenAlgorithm;

    @Override
    public void updated(Dictionary<String, ?> properties) {
//    	chosenAlgorithm = Algorithm.valueOf((String) properties.get(DemoOptimisationConfigurable.ALGORITHM_KEY));
    }

    // TODO jakub add methods for querying specific configuration properties if needed.
}
